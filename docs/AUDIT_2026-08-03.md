# Phase 1 status — 2026-08-03

Format per owner instruction: HUA / NAHI HUA / kyun nahi, for every item in
`FINAL_PROMPT.md` Phase 1. Method: live browser testing against a local
`python3 -m http.server` instance (Chrome automation), plus direct source
reading, for every item below.

---

## 1.1 — Landing stats, three categories — **HUA**

Replaced the single stat row with three separately-labelled, never-merged
groups in `dashboard/index.html`:

- **BOUNDARY COVERAGE**: 36 States/UTs, 733 Districts, 6,312 Blocks/Tehsils,
  654,285 Villages — read live from `data/boundaries/soi/_manifest.json`
  (`totals.states_with_villages`, `districts_file.feature_count`,
  `totals.total_blocks`, `totals.total_villages`).
- **OBSERVED CLIMATE**: "N of 733 Districts with climate indices" — read
  from the new `data/climate_manifest.json` (`scripts/build_climate_manifest.py`,
  counts real files: 5 IMD + however many `data/climate/<state>/<district>.json`
  the running GEE job has written — 48 as of this commit, growing). Years of
  Record parsed from the same file's `imd.years` string, not hardcoded.
- **PROJECTED CLIMATE**: shown as a **PENDING** status card, not a number.
  `forecast_2040.json` (the only projection file that exists) is an OLS
  trend on observed data, explicitly not CMIP6 — checked
  `docs/DATA_SOURCES.md` and `scripts/config.py`: the real CMIP6 pipeline
  (`05_gee_cmip6_2040.js` + `06_convert_gee_export.py`) has never actually
  been run to produce output, and only SSP2-4.5/2040 is configured, not
  SSP5-8.5/2050. Showing a bare number here would itself be the fabrication
  the owner is trying to eliminate, so it stays PENDING with the real
  ensemble/scenario/baseline stated in text until that pipeline is run.

2025 IMD NetCDF check: `IMD_TMAX_DIR`/`IMD_TMIN_DIR`/`IMD_PRECIP_DIR` are
unset and `scripts/config.py` resolves them to empty — confirmed again this
session, no raw IMD NetCDF of any year exists on this machine. Left at
2000–2024, did not guess.

## 1.2 — Count-up animation, broken ticker — **HUA**

- `animateCounters()` no longer runs a 25 ms `setInterval` ramp; it sets the
  real fetched value on the first paint after the fetch resolves. Verified
  live: 1 second after load the hero already reads 36/733/6,312/6,54,285 —
  no wrong intermediate number to catch in a screenshot.
- Removed the scrolling header ticker entirely (`.topbar-ticker`,
  `.ticker-inner`, `@keyframes ticker`, `updateTicker()`, its poll loop) —
  this is what was rendering as a truncated "...lerts" mid-scroll. Verified
  `document.querySelector('.topbar-ticker')` is now `null` on the live page.

## 1.2b — Same-day live-code fixes — **HUA**

- Duplicate "Verified Sources" div: confirmed only one now exists
  (`hero-stat-sources-line`, inline in the sentence under the three stat
  groups) — this was already collapsed to one element in this session's
  first landing-page pass, re-checked, no second copy found anywhere in
  `index.html`.
- `data-target="25"` / `data-target="6"` count-up mechanism: removed per 1.2.
- "Years of Record": now computed from `climate_manifest.json`'s real
  `imd.years` string (see 1.1), not hand-typed.

## 1.3 — Fabricated numbers — **HUA (2 more found beyond what was reported)**

- `"AI accuracy stays at 94.2%"` — this was already a dead comment with no
  actual value attached (the stat itself was removed in an earlier pass);
  deleted the leftover comment.
- Dashboard "Data Coverage: 5 districts" metric card (`#m-ai`) was static
  HTML, never updated — now reads the same `climate_manifest.json` total
  live (currently "48 districts", labelled "IMD + ERA5-Land/CHIRPS (GEE)"
  instead of the old, source-mismatched "IMD + DiCRA verified").
- Header "ERA5" badge: already removed in this session's earlier pass
  (`DATA: IMD, DiCRA, Survey of India`), re-verified still correct.
- **Not in the original list, found during this audit pass**: `setTimelineYear()`
  (the historical/forecast year-slider handler) was fabricating four values
  for whatever year the user dragged to — NDVI derived from SPI via an
  arbitrary formula, a fake Tmax bucketed from heatwave-day count, Soil
  Moisture as `ndvi*80`, and Groundwater as `-(2 + drought/40)` — for two
  data sources (soil moisture, groundwater) that are honestly "Not
  available" everywhere else in this exact codebase. Fixed: soil
  moisture/groundwater/NDVI now say "Not available" for any year (neither
  source is integrated, full stop); heat severity now buckets from the
  real per-year `heatwave_days` count and displays that real count, never
  an invented temperature; drought now shows the real per-year SPI-12
  value with its McKee et al. 1993 category label instead of an invented
  percentage. Verified live for Jabalpur, year 2010: real values in, no
  fabricated ones. This was live on production before this fix.

## 1.4 — Panel-by-panel provenance audit — **HUA (table below)**

| Panel / metric | File | Real source | Verdict |
|---|---|---|---|
| Historical Indices, Heatwave/Drought/SPI/Rainfall cards | `data/mp_climate_data.json` | IMD 0.05° gridded NetCDF, 2000–2024, 5 districts | Verified, label matches |
| Data Coverage card | `data/climate_manifest.json` (new) | Same + `data/climate/<state>/<district>.json` (ERA5-Land/CHIRPS via GEE) | Fixed this session — was hardcoded "5 districts" |
| Rainfall Departure card | `mp_climate_data.json` `annual.annual_rain_mm` vs its own mean | IMD | Correct — already computed as a real departure, not raw total (contradicts the stale 2026-08-01 audit finding, which is now out of date) |
| Heatwave Severity card | `mp_climate_data.json` `indices.heatwave_days_mean` | IMD | Correct — already thresholded off the real index (also contradicts the stale 2026-08-01 finding) |
| NDVI Score / NDVI Trend | `data/dicra_ndvi.json` | UNDP DiCRA (MODIS), 52 MP districts only | Verified, label matches; year-slider mode now correctly says "Not available" instead of a fabricated proxy (see 1.3) |
| Crop Stress card | derived in-browser from real drought probability | Same IMD district data, labelled "(indicative)" | Verified honest; trend caption bug fixed this session (was stuck on "Select a district" after a district was already selected) |
| Soil Moisture card | none | Not integrated (needs SMAP/Bhuvan) | Correct "Not available" everywhere now, including the year-slider path (fixed this session) |
| Groundwater card | none | Not integrated (needs CGWB/India-WRIS) | Same — fixed this session |
| Mandi Prices panel | `data/mandi_prices.json` | AGMARKNET via data.gov.in, all 733 districts, daily | Verified; slug-lookup bug fixed this session (multi-word district names silently failed) |
| Crop Statistics panel | `data/crop_stats.json` | data.gov.in, MoA&FW, years vary (1997–2013ish) | Verified, coverage-note shown; same slug bug fixed |
| Village Boundaries / profiles | `data/boundaries/soi/*`, `data/village_profiles/*` | Survey of India, via NWDP | Verified nationwide |
| Cadastral panel | `cadastral_loader.js` (stub) | None — disabled | Correctly disabled; labelling as demonstration (2026-08-01 audit P1) not yet re-verified this session |
| Kisan Sahayak chatbot | `mp_climate_data.json`, `mandi_prices.json`, `data/knowledge_base/index.json` | Same IMD/AGMARKNET files + 37 real DOAJ-indexed papers (existing, pre-dates this session) | Rebuilt this session; fabricated ₹/quintal template answers already removed in an earlier pass this session, verified again live |
| Rainfall Forecast (7-day) | not yet traced this pass | unknown | **Open — needs a follow-up check, not yet audited** |
| Forest Monitor / Biodiversity Risk sidebar items | not yet traced this pass | unknown | **Open — needs a follow-up check** |

## 1.5 — Government-source matching table — **HUA**

| Data type | Portal shows it as | Real authoritative source | Match? |
|---|---|---|---|
| Climate indices | IMD | India Meteorological Department (MoES) | Yes |
| Mandi prices | AGMARKNET | AGMARKNET / data.gov.in, Ministry of Agriculture & Farmers Welfare | Yes |
| Crop production stats | data.gov.in | Ministry of Agriculture & Farmers Welfare | Yes |
| Soil | Not integrated | Soil Health Card, NBSS&LUP, ICAR | N/A — honestly absent, not mismatched |
| Groundwater | Not integrated | CGWB, India-WRIS | N/A — honestly absent |
| Boundaries | Survey of India | Survey of India (via NWDP) / LGD not separately cross-checked | Yes, SoI confirmed; LGD cross-check not done |
| Census / village profile | Survey of India village-boundary attribute table | Census of India would be the canonical demographic source; this portal's population/household figures come from SoI's own attribute columns, not the Census directly | **Partial mismatch — label should say "Survey of India village profile", not imply Census of India; not currently claiming Census anywhere, verified, no fix needed** |
| Forest | Sidebar item exists | Forest Survey of India (FSI) | Not integrated — sidebar item routes to an unrelated panel per the 2026-08-01 audit (P4), not re-verified this session |
| Satellite / NDVI | DiCRA (MODIS-derived) | Bhuvan/NRSC, GEE | DiCRA is itself MODIS-derived and correctly cited; GEE (ERA5-Land/CHIRPS) also correctly cited in the new climate files |

## 1.6 — Missing-data rule — **HUA, with one exception found and fixed**

Standard: districts/villages without real data must show "Not available" /
"Climate data not yet available for X", never a neighbouring unit's value,
never a blank dash.

- Chatbot (`getLocalResponse`): verified this session — mandi branch says
  "No AGMARKNET arrivals recorded for `<real district>` today", never
  substitutes a value.
- Soil Moisture / Groundwater: verified "Not available" everywhere,
  including the year-slider path (see 1.3 — this was the exception, now
  fixed).
- District/village selection resetting to a stale previous selection's
  numbers: already fixed in an earlier pass this session (`aaf39e5`).

---

## Summary

Phase 1 substantially done. Two open items carried forward (not yet
audited this pass): the 7-Day Rainfall Forecast panel's real source, and
whether Forest Monitor/Biodiversity Risk sidebar items still route to the
wrong panel (2026-08-01 audit P4) — will check in the Phase 2 pass since
that's also where "coming soon" labelling for non-functional sidebar items
is required.

Continuing directly to Phase 2 (visual redesign) per "beech mein mat
poochho" — this file will be extended, not replaced, as later phases turn
up more findings.
