# Dashboard hosting
streamlit>=1.28.0

# Climate pipeline
numpy>=1.24
pandas>=2.0
scipy>=1.10
xarray>=2023.1
netCDF4>=1.6
pyarrow>=14.0
geopandas>=0.14
shapely>=2.0
topojson>=1.10
tqdm>=4.65
earthengine-api>=1.6
xlrd>=2.0          # crop_yield pipeline: reads the ICRISAT district panel's legacy .xls

# Kisan Sahayak RAG ingestion (scripts/10_ingest_kisan_manuals.py)
pdfplumber>=0.11   # NOTE 2026-08-08: this was assumed already present when this
                   # script was commissioned ("pdfplumber, already a dependency
                   # per requirements.txt -- check") -- it was NOT actually
                   # listed here before this line. Added now; verified importable
                   # in this environment (0.11.8) regardless.

# Testing
pytest>=7.4
